/**
 * How JS Works & Execution Context:
 *
 * everything in js happens inside an execution context
 *
 * in the execution context all the variables and function are stored
 * in the form of key value pair
 *
 * second component of execution context is code component
 * here the code run line by line we are also called it as
 * thread of execution
 *
 * javascript is synchronous single threaded launguage
 *
 * synchronous single threaded means one command at a time with a 
 * specific order
 * 
 * 

 */
